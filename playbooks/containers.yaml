- name: Start and Enable docker service
  ansible.builtin.service:
    name: docker
    state: absent
  register: dockerservice_out

- name: Print docker status
  ansible.builtin.debug:
    var: "{{ dockerservice_out.status.ActiveState }}"